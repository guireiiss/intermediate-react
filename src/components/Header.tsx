import { OnlineUsers } from "./OnlineUsers"

export const Header = ( ) => {
    return (
        <header>
            <h1 className="text-3xl">Titulo da pagina</h1>
            <OnlineUsers />
        </header>
    )
}